# Authors
#
# Used to override the default site author (set in _config.yml) on a
# per-post basis. Add an entry here and set `author: <Name>` in a post's
# front matter if you ever have a co-authored post.
